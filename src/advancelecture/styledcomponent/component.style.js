import styled from "styled-components";

const StyleComponentWrapper = styled.div`
  .myclass {
    background: black;
    color: white;
    text-align: center;
    padding: 15px;
  }
`;

export default StyleComponentWrapper;
